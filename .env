# Docker Hub Username
# Ganti dengan username Docker Hub Anda
DOCKER_USERNAME=denagus

# Database Configuration (sudah di-set di docker-compose.yml)
# Tidak perlu diubah untuk demo
POSTGRES_DB=eproc_demo
POSTGRES_USER=eproc
POSTGRES_PASSWORD=eproc_demo_2026

# API Configuration (sudah di-set di docker-compose.yml)
# Tidak perlu diubah untuk demo
JWT_SECRET=eproc_demo_jwt_secret_2026_change_in_production
PORT=6000
